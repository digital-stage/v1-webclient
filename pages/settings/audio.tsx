import React from 'react';
import Layout from '../../components/global/layout/Layout';
import AudioSettings from '../../components/old/settings/AudioSettings';

const AudioSettingsPage = (props: {}) => {
  return (
    <Layout>
      <AudioSettings />
    </Layout>
  );
};
export default AudioSettingsPage;
